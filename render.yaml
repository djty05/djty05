services:
  - type: web
    name: marketplace-scanner
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
      # Optional: Add notification keys
      # - key: PUSHBULLET_API_KEY
      #   value: your-key-here
